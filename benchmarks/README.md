# Benchmarks

`qualitative_redteam_cases.yaml` defines deterministic qualitative cases used by:

```bash
python -m src.generate_demo_benchmark
```

The command generates public demo artifacts under `reports/demo_benchmark/` without external API calls.
